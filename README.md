# TestPrimo (Run Project)
1. Clone Repository
   `git clone https://github.com/KKhimmoon/TestPrimo.git`
2. Install Dependencies
   `npm install`
3. Start Application
   `npm start`
4. Run test
   `npm test`
